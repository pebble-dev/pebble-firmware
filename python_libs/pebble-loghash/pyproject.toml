[project]
name = "pebble.loghash"
version = "2.6.0"
description = "Pebble Log Hashing module"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pebble"]
